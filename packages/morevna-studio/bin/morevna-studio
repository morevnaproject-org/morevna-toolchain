#!/bin/bash

source $HOME/.config/morevna-studio/morevna-studio.conf

if [ "$USE_0INSTALL" == "0" ]; then
    "$MOREVNASTUDIO_LAUNCHER" exec "$APP" "$@"
else
    0install run https://.../morevna-studio.xml exec "$@"
fi
